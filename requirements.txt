# Core
Django==5.0.14
dj-database-url==3.1.2      # DATABASE_URL parsing; swap SQLite for Postgres/MySQL later
python-dotenv==1.2.3        # loads .env for local secrets/config

# Telegram bot (notifications, meeting booking, attendance confirmation)
python-telegram-bot==21.11.1
requests==2.34.2            # used by bot/notify.py for simple sendMessage calls
